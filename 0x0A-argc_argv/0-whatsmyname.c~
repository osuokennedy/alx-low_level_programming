#include <stdio.h>
/**
 *main - This is a function that prints its name
 @argc: argc parameter
 *@argv: An array o a command listed
 *Return: 0 Always Success
 */
int main(int argc, int argv[0])
{
printf("%s\n", argc);
return (0);
}
