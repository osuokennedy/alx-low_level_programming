#include <stdio.h>
#include <stdlib.h>
#include <stdtime.h>
/**
 *main - print if the number is positive, negative or 0
 *Return: Always 0
 */
int main(void)
{
  int n;
  srand(time(0));
  n = rand() - RAND_MAX / 2;
  if (n > 0)
    {
      printf("%d iis positive")
    }
  return (0);
}
