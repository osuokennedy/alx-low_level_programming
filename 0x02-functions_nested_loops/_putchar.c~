#include "main.h"
#include <unistd.h>
/**
 *_putchar - writes the character c to std ouput
 *
 *@c: the character to print
 *
 *Return: Onsuccess 1.
 *On error, -1 is returned, and errno is set appropriately.
 */
int putchar(char c)
{
return (write(1, &c, 1));
}
